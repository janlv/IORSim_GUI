name: Build Workflow

on:
  workflow_dispatch:
#    inputs:
#    tags:
#    description: 'test build'
  # release:
  #   types: [ published ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    - run: |
        python -m pip install --upgrade pip
        python -m pip install -r requirements.txt
        python -m pip install pyinstaller
        curl.exe -o upx-3.96-win64.zip https://github.com/upx/upx/releases/download/v3.96
        7z e upx-3.96-win64.zip
        .\compile_iorsim.bat
    - uses: actions/upload-artifact@v3
      with:
        name: IORSim_GUI.exe
        path: .